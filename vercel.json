{
  "installCommand": "cd frontend && npm install",
  "buildCommand": "cd frontend && npm run build:vercel",
  "outputDirectory": "frontend/dist/foro-frontend/browser",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
